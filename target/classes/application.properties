# -----------------------------------------------------------------------------
# Spring Boot configuration for the IAM microservice
#
# The following properties configure the database connection and JPA settings.
# Replace the username and password with credentials appropriate for your
# local MySQL installation. If you prefer to experiment without installing
# MySQL, consider using the H2 in-memory database by including the H2
# dependency and changing the datasource URL to `jdbc:h2:mem:testdb`.
# -----------------------------------------------------------------------------

spring.datasource.url=jdbc:mysql://iamservice.cqz6seome3ml.us-east-1.rds.amazonaws.com:3306/iam_service?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Mclaren07Ferrari
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
server.port=${PORT:8081}
# Configure logging level for debugging (optional)
logging.level.org.springframework.security=INFO
# When deploying, you may wish to externalize sensitive values such as the JWT
# secret and expiration time. For this example these values are hard-coded in
# JwtUtil for simplicity.